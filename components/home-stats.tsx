'use client';
import Link from 'next/link';
import { BarChart3 } from 'lucide-react';
import { usePlayerStore } from '../lib/player-store';
export function HomeStats(){const plays=usePlayerStore(s=>s.playCounts),seconds=usePlayerStore(s=>s.listenSeconds);return <section style={{margin:'28px 0',padding:22,border:'1px solid #302b49',borderRadius:22,background:'linear-gradient(135deg,#171426,#111119)'}}><div style={{display:'flex',alignItems:'center',gap:14}}><div style={{flex:1}}><p className="eyebrow">YOUR LISTENING</p><h2>สถิติการฟัง</h2><span style={{color:'#9d9aaa',fontSize:12}}>สรุปการฟังของคุณในเครื่องนี้</span></div><BarChart3 size={24}/><Link href="/stats" className="text-button">ดูทั้งหมด</Link></div><div style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:12,marginTop:18}}><div className="stat-card"><strong>{Object.values(plays).reduce((a,b)=>a+b,0)}</strong><span>ครั้งที่เปิดเพลง</span></div><div className="stat-card"><strong>{Math.floor(Object.values(seconds).reduce((a,b)=>a+b,0)/60)} นาที</strong><span>เวลาที่ฟังรวม</span></div><div className="stat-card"><strong>{Object.keys(plays).length}</strong><span>เพลงที่เคยฟัง</span></div></div></section>}
